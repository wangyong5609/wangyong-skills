---
name: cimidata-wechat-article-collector
description: 查询、采集并本地归档微信公众号数据：按账号、文章链接或日期范围获取历史文章、正文、互动指标、脱敏评论、关键词搜索和爆文榜单。Use when the user asks to 采集公众号文章、获取正文、按年份归档、查阅读点赞在看、搜索公众号文章或查看微信爆文；不用于微博、抖音或头条数据。
---

# 次幂公众号数据

用户只需描述公众号、文章或日期范围。不要让用户学习命令、`wxid`、`last_id`、token、SQLite 或接口参数。

它与 `wechat-article-collector` 完全独立：不要调用极致了/大加啦接口，也不要混用凭据。

## 第一次使用

先检查是否已经配置。没有配置时，读取 [第一次使用](references/first-use.md)，严格按其中的逐步话术带用户操作：一次只说一步，等待用户完成后再继续。

当用户把 AppID 和 Secret 发给 AI 后，AI 将它们写入本 Skill 根目录 `.env`，免费验证后只回答“已经存好”。绝不在回复、日志、输出或 Git 中重复这两个值。

## 采集方式

用户只给公众号名称时，默认采最近 1 篇正文；不采评论、图片或互动数据。先用一句大白话报出本次最高花费，等用户回答“可以”再执行：

```text
我先帮你采最近一篇正文，不采评论和图片。本次最多花 ¥X，可以吗？
```

用户给文章链接时，默认只取正文。只有用户明确说“要阅读量/点赞”“要评论”“要图片”“多采几篇”时，才增加对应范围和费用。

用户给出日期或年份时，使用 `collect-range`，分两阶段执行：

1. 先以 `--metadata-only --dry-run` 计算本轮历史查询上限，默认最多 100 页；把金额换成大白话让用户确认。
2. 确认后保存每页结果和断点。未到目标日期时报告“已查到哪天、已花多少、下一轮最多多少”，再次确认后继续。
3. 找到完整范围后，准确计算缺失正文数量和费用；再次确认才获取正文。已缓存正文不重复购买。

本地索引由 Python 自带的 SQLite 自动创建，无需安装或配置，也不要主动向普通用户解释。同名公众号时展示少量候选让用户选择；不要让用户自行找 `wxid`。

## AI 内部规则

- 具体字段、端点、单价和调用频率：读取 [接口映射](references/cimidata-api.md)。
- 每次计费前先 `--dry-run`，然后只在用户确认后使用 `--confirm-paid --max-cost`。
- 日期按北京时间闭区间理解；脚本内部转换为半开区间，避免漏掉结束当天。
- 因预算、重复页或接口错误中止时必须报告 `partial`，保留断点，不得声称完整。
- 评论默认关闭；启用时只保留一级、点赞不少于 10 的前 10 条，仅含正文和点赞数。
- 凭据先读环境变量；没有时读 Skill 根目录 `.env`。用户手动配置可参考 `.env.example`，但不要把这个文件路径抛给普通用户。

接口能力或成本需要复核时，再读取 [日期范围接口研究](references/cimidata-time-range-api-research.md)。

## 完成时

只告诉用户：是否完成、采到几篇、花了多少、保存在哪里，以及是否需要继续。不要输出凭据、token、内部游标或原始评论身份字段。
